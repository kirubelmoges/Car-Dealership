services:
  # Frontend - inside server/frontend
  - type: static
    name: car-dealership-frontend
    rootDir: server/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: CI
        value: false
      - key: NODE_VERSION
        value: 18

